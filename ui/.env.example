# ---------------------------------------------------------------------------
# Docs Pipeline maintainer UI configuration.
# Read by Vite; anything the browser needs must be prefixed with VITE_.
# Copy to ui/.env for local overrides.
# ---------------------------------------------------------------------------

# Backend + Marqo proxy targets used by the Vite dev server (see vite.config.js).
# For local non-Docker runs use localhost targets (below).
VITE_API_PROXY_TARGET=http://localhost:8001
VITE_MARQO_PROXY_TARGET=http://localhost:8882

# --- Keycloak / OIDC authentication ----------------------------------------
# When false (the default) the UI runs fully open with no login — handy for
# local development without Keycloak. Set to true to require sign-in.
VITE_AUTH_ENABLED=false

# --- Feature flags ---------------------------------------------------------
# Document validity: shows the "Valid from" / "Valid until" fields beside the
# document type, letting a reviewer set how long a document answers searches.
# The backend stamps and filters on a validity period regardless; this only
# exposes the entry fields. Turn on when the business adopts the feature.
VITE_DOCUMENT_VALIDITY_ENABLED=false

# DEV: platform realm with Google SSO + multi-state groups
VITE_KEYCLOAK_URL=https://dev-auth-vistaar.da.gov.in/auth
VITE_KEYCLOAK_REALM=bharat-vistaar
VITE_KEYCLOAK_CLIENT_ID=bharat-vistaar
VITE_KEYCLOAK_IDP_HINT=google

# PROD example:
# VITE_KEYCLOAK_URL=https://auth-vistaar.da.gov.in/auth
# VITE_KEYCLOAK_REALM=bharat-vistaar
# VITE_KEYCLOAK_CLIENT_ID=bharat-vistaar

# Keycloak client Valid Redirect URIs must include (with /docs-pipeline base in prod):
#   https://<host>/docs-pipeline/auth/sso-callback
#   https://<host>/docs-pipeline/login
# Local (base /):
#   http://localhost:<port>/auth/sso-callback
#   http://localhost:<port>/login

# Production Vite base (baked into UI image):
# VITE_BASE=/docs-pipeline/
# VITE_API_BASE=/docs-pipeline-api
